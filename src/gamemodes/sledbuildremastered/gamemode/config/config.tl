local COLORS = require("configs/colors")
local CONSOLE = require("configs/console")
local DISCORD = require("configs/discord")
local TEAMS = require("configs/teams")

global CONFIG = {
  COLORS = COLORS, 
  CONSOLE = CONSOLE,
  DISCORD = DISCORD,
  TEAMS = TEAMS
}