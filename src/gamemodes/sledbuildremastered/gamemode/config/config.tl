local COLORS = require("config/configs/colors")
local CONSOLE = require("config/configs/console")
local DISCORD = require("config/configs/discord")
local TEAMS = require("config/configs/teams")

global CONFIG <const> = {
  COLORS = COLORS, 
  CONSOLE = CONSOLE,
  DISCORD = DISCORD,
  TEAMS = TEAMS
}
